# AISLE 计划一：S1 零售任务自主改进

## 当前仓库状态

CUDA 后端与演示相关改动已经提交到本地：

```text
f0a7cab feat: enable automatic CUDA simulation backend
```

本地 `main` 比 `origin/main` 领先一个提交，目前没有推送到远程仓库。

仅仅让模拟器持续运行并不会自动改进机器人行为。Genesis 提供的是执行环境和
实验数据，而不是学习机制。要实现持续改进，需要研究智能体修改策略与规划节点、
运行 rollout、分析失败，并将成功行为保存为可复用技能。

## 第一个改进场景

首先改进使用移动底盘和 Franka 机械臂的 **S1 零售订单拣选任务**。

机器人需要：

1. 接收包含两类商品及相应数量的订单。
2. 从交付柜台导航到正确的货架位置。
3. 拣取正确商品。
4. 返回交付柜台。
5. 交付全部所需商品，并且不能交付任何多余商品。

相比继续运行 T0，S1 更适合作为下一阶段目标，因为 T0 已经基本解决。S1 能够
检验项目最重要的研究主张：

- 移动导航；
- 长时序规划；
- 多次拣取和交付；
- 精确的商品和数量选择；
- 失败后的恢复；
- 可迁移到 S2 和 S3 的复用技能。

## 实验目标

> 将 S1 移动订单拣选图在开发种子上的 pass@1 提升到至少 0.80，并在 8 个
> 留出种子上达到至少 0.75；同时保持 `extra_item` 失败为零、未经过安全门控
> 的运动违规为零。

次要指标：

- 减少 `missing_item`、`dropped` 和 `timeout`。
- 记录首次成功所需时间。
- 记录所消耗的 token 数和 episode 数。
- 保持初始场景状态的确定性。
- 提取至少一个被最终图实际使用的复用技能。

此前 H3 的 S1 留出集结果只有 0.375 和 0.500，因此该目标具有明确的改进空间。

## 技术路线

AISLE 继续作为核心运行时和评估框架。ENPIRE 与 ASPIRE 提供互补的研究方法，
而不是取代 AISLE。

### ENPIRE 风格的外层循环

使用 Codex 或 Claude 等外部编码智能体执行实验：

```text
检查图和 trace
        ↓
提出一个假设
        ↓
修改规划器或技能节点
        ↓
验证类型化数据流图
        ↓
在少量种子上运行
        ↓
分析结构化失败类型
        ↓
保留、修订或否定该想法
```

这一以假设为驱动的循环才是持续改进的实际机制。

### ASPIRE 风格的技能积累

当智能体发现有效行为后，将其提取为带版本和评估证据的技能。

建议优先形成以下技能：

- `navigate-to-zone`：导航到指定区域
- `shelf-approach`：安全接近货架
- `pick-retail-item`：拣取零售商品
- `return-to-counter`：返回交付柜台
- `place-on-counter`：将商品放到柜台
- `recover-dropped-item`：处理掉落商品
- `placement-controller`：精确放置控制

这些技能构成从 S1 向以下任务迁移的通道：

- S2：货架补货；
- S3：检测并纠正放错位置的商品。

### AISLE 的作用

AISLE 为整个研究循环提供结构化保障：

- 类型化 dora 数据流图组合；
- 启动前验证；
- 冻结的验证器和环境；
- 受安全门控的运动；
- 结构化失败分类；
- 基于种子的 rollout；
- Arrow trace 和视频；
- 留出集评估；
- 想法记录和实验预算；
- 运行中节点替换。

推荐的整体组合为：

```text
Codex 或 Claude 研究智能体
        +
ENPIRE 风格的假设循环
        +
ASPIRE 风格的持久技能库
        +
AISLE 类型化运行时、安全与评估
        +
Genesis CUDA 模拟
```

## 感知策略

第一阶段先使用 oracle 感知，不要立即引入 VLM。

初始实验应集中改进规划、导航、操作和失败恢复，避免同时混入感知误差。当
oracle S1 能够稳定达到目标后，再进入真实感知层级：

- 使用 OCR 或 VLM 阅读订单；
- 使用商品检测器识别货架商品；
- 使用 planogram 进行货架位置定位；
- 为 S3 引入错放商品检测器。

后续可以使用小型 VLM 提供语义感知，但不应让 VLM 直接控制底层机器人运动。
VLM 应输出结构化观测，再交给确定性规划器和受安全门控的控制器。

## 分阶段路线图

### 阶段一：Oracle S1 订单拣选

目标：开发集 pass@1 至少 0.80，留出集 pass@1 至少 0.75，并保持
`extra_item` 为零。

### 阶段二：稳定可复用技能

目标：确保最终 S1 数据流图真正使用已经注册和评估的技能。

### 阶段三：迁移到 S2 补货

目标：复用导航、抓取和放置技能，只学习补货任务特有的行为。

### 阶段四：迁移到 S3 错放纠正

目标：复用 S1/S2 技能，并尽可能减少新增策略代码。

### 阶段五：真实感知

目标：使用 OCR、VLM 和检测节点替换 oracle 的订单与商品输入，同时保留
oracle 验证器作为参考标准。

### 阶段六：实体机器人

目标：在真实硬件上复用同一套类型化数据流图、能力契约、安全门控和评估结构。

## 第一棵改进树

研究智能体应依次调查：

1. 任务规划器是否完成全部商品和数量要求。
2. 导航与操作的时序：机械臂伸出时不得移动底盘。
3. 运输前是否可靠确认抓取成功。
4. 柜台放置是否能够防止商品掉落。
5. 未抓取成功或商品掉落后的恢复策略。
6. 避免将任何非订单商品移动到柜台。

第一个实验假设建议为：

> 在开始导航前增加明确的抓取确认，并允许一次有上限的重试，可以减少
> `missing_item` 和 `dropped`，同时不会增加 `extra_item`。

先在 4 个开发种子上检验该假设，再投入更大的实验预算。

## 基础设施前置条件

在启动长时间自主实验前：

1. 通过符合项目章程的可选 `cuda` extra，使 CUDA PyTorch 安装可复现。
   当前 CUDA wheel 仍然只是本地 `.venv` 覆盖。
2. 在每个运行 manifest 中记录后端和 GPU 来源信息：
   - Genesis 后端；
   - GPU 型号；
   - NVIDIA 驱动版本；
   - PyTorch 版本；
   - CUDA runtime 版本；
   - GPU 峰值显存。
3. 在选定的开发种子和留出种子上建立干净的 S1 基线。
4. 同一时间只运行一个模拟器工作负载。

## 推荐的第一次自主实验

在 AISLE 研究契约约束下改进 oracle S1 移动订单拣选任务，然后将成功的导航、
抓取、恢复和放置行为提升为可供 S2 与 S3 复用的技能。

