# AISLE 机器人 Harness Engineering 对比计划

## 核心区别

ASPIRE、ENPIRE 和 Harness VLA 主要提出如何改进机器人行为。AISLE 研究的是：
机器人行为周围的工程 harness，如何改变改进过程的可靠性、安全性、速度和科学
可信度。

AISLE 的目标不是再发明一个规划器或策略模型，而是构建一个基础设施，使规划器、
编码智能体、VLA 和技能学习方法能够在其中安全、可测量地运行。

## 策略与 Harness 的区别

机器人策略回答：

> 给定当前观测和目标，机器人下一步应该做什么？

机器人 harness 回答：

> 这个策略如何连接、约束、观测、评估、修复、替换和比较？

如果没有强 harness，编码智能体可能会生成一个同时包含感知、规划、运动、重试、
日志和评估的 Python 脚本。任务失败时，智能体只知道“任务失败”，却不知道是哪
一个子系统造成失败。

AISLE 将行为系统拆分为类型化节点：

```text
感知
 ↓
任务规划器
 ↓
导航 / 抓取规划器
 ↓
运动执行器
 ↓
安全预算门控
 ↓
机器人或模拟器
```

同时运行受保护的基础设施：

```text
重置服务
验证器
trace 记录器
环境哈希
失败分类器
实验账本
```

需要验证的主张是：这种结构化拆分能够比修改单体脚本更有效地支持智能体机器人
工程。

## 与相关系统的关系

### ENPIRE

ENPIRE 最接近 AISLE 的外层研究循环。它将 Physical Autoresearch 定义为编码
智能体提出假设、修改机器人策略、运行实验并读取结果的过程。

AISLE 可以作为 ENPIRE 风格智能体下面的执行基础：

```text
ENPIRE 风格研究智能体
             ↓
AISLE 验证器与实验 harness
             ↓
dora 节点
             ↓
Genesis 或实体机器人
```

两者关注点不同：

- ENPIRE：自主智能体能否改进真实机器人策略？
- AISLE：类型化中间件能否让这种改进更快、更安全、更容易诊断和复现？

参考：[ENPIRE](https://arxiv.org/abs/2606.19980)。

### ASPIRE

ASPIRE 关注通过机器人程序修复和持续扩展的技能库实现终身学习。其执行引擎提供
详细 trace；经过验证的修复被保存为可复用技能，进化搜索则产生新的任务和程序。

AISLE 与它的关系为：

```text
ASPIRE 风格技能发现
             ↓
AISLE 技能注册和 evalcard
             ↓
类型化能力 manifest
             ↓
在后续数据流图中验证并复用
```

两者关注点不同：

- ASPIRE：成功修复如何积累为可迁移技能？
- AISLE：技能组合与复用如何实现类型检查、验证、安全门控和实验审计？

AISLE 的 H3 实验尝试验证这一优势，但没有达到目标。因此当前技能机制尚未证明
持久技能库能够让后续任务更快完成。

参考：[ASPIRE](https://arxiv.org/abs/2607.00272)。

### Harness VLA

Harness VLA 将冻结的 VLA 作为可重试的接触密集型原语。记忆引导的规划器学习
何时以及如何调用 VLA，而解析式原语负责定位、准备、运输、导航和释放。

AISLE 可以承载这种架构：

```text
记忆引导规划器节点
          ↓
解析式导航 / 准备节点
          ↓
用于接触操作的冻结 VLA 节点
          ↓
AISLE 安全预算门控
          ↓
机器人
```

区别在于：

- Harness VLA：围绕冻结 VLA 构建的特定行为 harness。
- AISLE：组合和评估多种策略类型的通用系统 harness，其中也可以包括
  Harness VLA。

AISLE 可以为 VLA 增加以下约束和证据：

- 允许哪些输入与输出 schema？
- 该 VLA 是否被授权控制当前机器人形态？
- 动作是否满足关节和工作空间限制？
- 是否违反推理时间或带宽预算？
- 是否能够访问特权 oracle 状态？
- 实际执行的是哪个版本？
- 是否可以独立替换该节点？
- 修改是否提高了留出集表现？

参考：[Harness VLA](https://arxiv.org/abs/2607.08448)。

## AISLE 可能具有的独特性

### 1. 类型化组合

每个节点声明输入、输出、Arrow schema、频率、机器人形态、能力以及代码或软件包
来源。验证器可以在消耗模拟或实体机器人 episode 之前拒绝无效系统。

这使编码智能体生成的组合能够被机器检查。

### 2. 结构化安全

安全机制位于策略之外：

- 所有运动必须经过预算门控。
- oracle 状态不能连接到策略节点。
- 冻结的环境、验证器和重置代码经过哈希检查。
- 不安全的数据流图拓扑在启动前被拒绝。

策略不能只“承诺安全”；数据流图结构会执行部分安全约束。

这与
[Harness Engineering for Physical AI](https://arxiv.org/abs/2606.09416)
中的 Projection 概念类似：机器人中间件负责组合控制、计算和通信约束。

### 3. 失败定位

单体脚本可能只报告：

```text
任务失败
```

AISLE 可以报告：

```text
数据流图启动成功
目标位姿以 15 Hz 到达
抓取目标在 t=4.2 开始
夹爪闭合，但物体没有移动
失败类型 = never_grasped
```

这会缩小编码智能体的搜索空间，使其只修改抓取或确认节点，而不必重写导航和
验证。

### 4. 受控实验

AISLE 记录：

- Git revision；
- 数据流图哈希；
- 环境哈希；
- 依赖指纹；
- 平台和后端；
- 随机种子；
- episode 结果和失败类型；
- trace 和视频；
- 实验想法与假设；
- token、episode 和时间预算。

这将“智能体改进了机器人”转化为可审计实验。

### 5. 可替换单元

Hot-swap 允许替换规划器或控制器，而无需重启完整数据流图。H4 在 T0 上测得
hot-swap 中位时间为 32.4 秒，重新启动为 41.8 秒。更大的潜在收益来自初始化
需要数分钟的复杂场景。

### 6. 与策略类型无关

AISLE 节点可以包含：

- 解析式控制；
- 运动规划；
- 编码智能体生成的程序；
- VLM；
- 冻结 VLA；
- 强化学习策略；
- Harness VLA 规划器；
- ASPIRE 发现的技能。

外围的验证、安全、trace 和评估机制保持不变。

## AISLE 必须完成的关键实验

真正重要的问题不只是：

> AISLE 能否解决 S1？

真正的问题是：

> 在使用同一个编码智能体、同一个机器人任务、相同初始能力以及相同计算和
> episode 预算时，AISLE harness 是否比等价的单体脚本环境产生更好的工程结果？

目前还没有足够强的证据回答这一问题。

## Harness 与脚本的匹配对照实验

### 条件 A：AISLE Harness

编码智能体可以使用：

- 类型化节点注册表；
- 数据流图验证器；
- 结构化失败分类；
- 每个 topic 的 trace；
- 可热替换节点；
- 冻结的验证器和重置服务；
- 技能注册；
- 实验账本。

### 条件 B：脚本基线

同一个智能体可以使用：

- 相同模拟器；
- 相同机器人 API；
- 相同任务；
- 相同验证器结果；
- 相同观测；
- 相同 token、时间和 episode 预算；
- 一个可编辑的 Python 策略脚本。

不能故意削弱脚本基线。它应获得合理日志和相同的非特权观测。实验测试的差异
应是结构化 harness，而不是基本可用性。

## 评估指标

| 指标 | 意义 |
|---|---|
| 留出集 pass@1 | 最终行为质量 |
| 首次成功时间 | 工程速度 |
| 首次成功所需 episode | 机器人/样本效率 |
| 首次成功所需 token | 智能体效率 |
| 无效启动次数 | 组合可靠性 |
| 失败定位准确率 | 诊断价值 |
| 错误/多余商品率 | 安全结果 |
| 未经过门控的违规 | 结构化安全 |
| 回归次数 | 修复是否破坏其他组件 |
| S2/S3 技能复用 | 技能迁移 |
| 重启与 hot-swap 时间 | 迭代开销 |
| 人工干预次数 | 自主程度 |
| 可复现重跑比例 | 科学可靠性 |

一个有意义的结果示例：

```text
相同编码智能体、相同 S1 任务、相同 40 小时预算

脚本条件：
- 留出集 pass@1 = 0.50
- 首次成功需要 34 个 episode
- 3 次回归
- 2 次 extra_item

AISLE 条件：
- 留出集 pass@1 = 0.80
- 首次成功需要 15 个 episode
- 1 次回归
- 0 次 extra_item
```

只有这种匹配对照实验，才能有力支持 harness engineering 能够改进机器人工程
的主张。

## 当前证据的诚实评价

- H1：类型化组合生成了 schema 合法的数据流图，但由于缺失依赖，启动成功率
  未达到目标；这一结果推动了 `INSTALL_MISSING` 验证。
- H2：智能体迭代循环产生了高性能系统。
- H3：技能积累在给定预算下没有达到目标。
- H4：T0 上 hot-swap 比重新启动更快，但证据规模有限。
- H5：主要实验记录中的错误商品安全表现较好。

AISLE 已经展示了有价值的基础设施，但尚未证明完整的核心主张：

> 类型化数据流 harness engineering 优于基于脚本的智能体机器人开发。

## 推荐的研究架构

将 ASPIRE、ENPIRE 和 Harness VLA 作为运行在 AISLE 内部的工作负载：

```text
ENPIRE 风格编码智能体
        ↓
ASPIRE 风格技能发现和记忆
        ↓
在适合学习控制的接触阶段使用 Harness VLA
        ↓
AISLE 类型化组合与中间件约束
        ↓
Genesis 或实体机器人
```

然后消融 AISLE 层：

```text
同一智能体 + 同一方法 + AISLE
对比
同一智能体 + 同一方法 + 单体脚本
```

## 推荐的下一项实验

在投入大量资源进行真实感知或实体机器人实验之前，先完成一个严格的 S1 harness
消融实验：

> 在达到相同或更高留出集 S1 成功率的同时，AISLE 是否能够减少时间、token、
> 无效启动、回归和安全失败？

如果答案是肯定的，AISLE 就具有明确的系统研究贡献：它不一定是更好的机器人
大脑，但它是一个能够让机器人智能安全、自主改进的更好工程环境。

